# Dependencies for driver_earnings_analysis.ipynb (local analysis notebook).
# Auth is via Application Default Credentials: gcloud auth application-default login
google-cloud-bigquery>=3.27
db-dtypes>=1.3          # required for BigQuery -> pandas .to_dataframe()
pandas>=2.0
matplotlib>=3.8
jupyterlab>=4.0
